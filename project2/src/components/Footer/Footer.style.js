import styled from "styled-components";

export const Footer = styled.footer`
    background: #202942;
    padding: 6rem 0;
    position: relative;
    color: #adb5bd;

    svg {
        font-size: 2rem;
    }

    .footer-list {
        li {
            padding: 1rem 0;
            font-size: 1.6rem;
            a {
                color: white;
            }
        }
    }
`;

export const FontLG = styled.p`
    font-size: 1.6rem;
    line-height: 1.5;
`;
